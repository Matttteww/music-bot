# База данных и стабильность бота

## 1. Сохранение базы данных при перезапуске

### Как работает сейчас
- Бот использует **SQLite** (файл `music_ratings.db`)
- SQLite сохраняет данные на диск при каждом `commit()` — данные не теряются при штатном перезапуске
- **НО на Bothost:** на **бесплатном** тарифе данные **стираются при перезапуске** (эпистемное хранилище)

### Решения

| Вариант | Описание |
|---------|----------|
| **Bothost Basic (99₽/мес)** | Подключаешь Volume — постоянный диск. Файл `music_ratings.db` сохранится. |
| **Bothost Pro (250₽/мес)** | То же + managed PostgreSQL (можно мигрировать позже) |
| **Локально** | Файл `music_ratings.db` в папке проекта — сохраняется при перезапуске |

### Путь к БД
В `config.py`: `DB_PATH = "music_ratings.db"` — рядом с кодом.

Для Bothost с Volume укажи путь к смонтированному диску через переменную окружения:
```
DB_PATH=/data/music_ratings.db
```

---

## 2. Резервное копирование (бэкапы)

### Ручной бэкап
```bash
# Вариант 1: скрипт
python backup_db.py

# Вариант 2: вручную скопировать файл
# music_ratings.db -> music_ratings_backup.db
```

### Регулярные бэкапы на Bothost
- **Basic/Pro**: настрой авто-бэкапы в панели Bothost
- Добавь задачу (cron) или команду админа для копирования `music_ratings.db` в облако (Google Drive, Dropbox и т.п.)

---

## 3. Проверка кода на возможные ошибки

### Уже обработано корректно
- **Payment polling** — бесконечный цикл с `try/except`, при ошибке логирует и продолжает
- **YooKassa** — вызовы в `run_in_executor`, ошибки обрабатываются
- **Подписка** — `get_chat_member` в try/except, при ошибке считаем «не подписан»

### Потенциальные риски

| Риск | Где | Рекомендация |
|------|-----|--------------|
| **Таймаут YooKassa** | `payments.py` — API без timeout | Добавить `asyncio.wait_for(..., timeout=15)` |
| **Таймаут Telegram API** | `send_message`, `get_chat_member` | aiogram по умолчанию ~60 сек — обычно достаточно |
| **Потеря FSM-состояния** | `MemoryStorage` | При перезапуске сбрасываются «загрузка трека», «голосование» — пользователь начинает заново. Это нормально. |
| **Большие исключения** | Много `except Exception: pass` | Лучше логировать: `logger.exception(...)` |
| **Отправка аудио** | `send_audio` с `file_id` | Telegram хранит файл — надёжно |

### Проверка перед деплоем
```bash
# Запуск бота локально
python main.py

# Прогон тестов (если есть)
pytest
```

---

## 4. Что может «застопить» бота

1. **Нет интернета** — бот не получит апдейты
2. **Telegram API недоступен** — редкие длительные сбои
3. **Необработанное исключение** — бот упадёт. Bothost перезапустит, но на FREE данные БД пропадут
4. **BOT_TOKEN неверный** — бот не запустится
5. **Нехватка RAM (256 MB на FREE)** — при большой нагрузке возможен OOM

---

## 5. Краткий чек-лист

- [ ] Если нужна сохранность БД на Bothost — тариф Basic+ и Volume
- [ ] Переменная `DB_PATH` на Volume: `/data/music_ratings.db`
- [ ] Регулярные бэкапы `music_ratings.db`
- [ ] Логи в Bothost смотреть при странном поведении
